# ========================================
# SECURITY WARNING - DEVELOPMENT SECRETS
# ========================================
# This file contains development credentials that MUST be changed before production!
#
# Required changes for production:
# 1. Generate strong secrets for HYDRA_SYSTEM_SECRET and KRATOS_SYSTEM_SECRET using:
#    openssl rand -base64 32
# 2. Change PostgreSQL passwords in both DSN strings (currently: Kennwort1)
# 3. Update OAuth client secret in hydra-setup/setup.sh (currently: dev-secret)
# 4. Configure production SMTP server (currently using mailslurper)
# 5. Set up proper SSL certificates (currently using mkcert)
#
# DO NOT commit production secrets to version control!
# ========================================

# generate this with `openssl rand -base64 32`
HYDRA_SYSTEM_SECRET=
HYDRA_DSN=postgres://admin:Kennwort1@postgres:5432/hydra?sslmode=disable
KRATOS_SYSTEM_SECRET=
KRATOS_DSN=postgres://admin:Kennwort1@postgres:5432/kratos?sslmode=disable

# for google oauth2
GOOGLE_OAUTH2_CLIENT_ID=
GOOGLE_OAUTH2_CLIENT_SECRET=
