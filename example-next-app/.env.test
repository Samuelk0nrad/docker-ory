NEXT_PUBLIC_APP_DOMAIN=https://auth.moorph.local # The public URL of the Next.js app, used for constructing callback URLs. Should be the same as the URL you use to access the app in the browser (e.g. http://localhost:3000 or https://myapp.com).
NEXT_INTERNAL_URL=http://nextjs-app # The internal URL of the Next.js app, used for server-side API calls. Should point to the Next.js container/service within the Docker network (e.g. http://nextjs-app or http://localhost:3000 if not using Docker).

NEXT_PUBLIC_KRATOS_PUBLIC_URL=https://auth.moorph.local/api/.ory/kratos
NEXT_PUBLIC_HYDRA_PUBLIC_URL=https://auth.moorph.local/api/.ory/hydra

KRATOS_PUBLIC_BASE_URL=http://ory-kratos:4434
HYDRA_PUBLIC_BASE_URL=http://ory-hydra:4444
HYDRA_ADMIN_URL=http://ory-hydra:4445

NEXT_PUBLIC_OAUTH_CLIENT_ID=frontend-app
# OAuth2 Client Credentials (server-side only, never expose to browser)
OAUTH_CLIENT_ID=frontend-app
OAUTH_CLIENT_SECRET=dev-secret


# Sentry Error Tracking & Performance Monitoring
# Public DSN used for client, server, and edge error reporting
NEXT_PUBLIC_SENTRY_DSN=https://x@x.ingest.de.sentry.io/x
# Sentry organization slug (for source map uploads during build)
SENTRY_ORG=
# Sentry project slug (for source map uploads during build)
SENTRY_PROJECT=
# Sentry auth token (required for source map uploads; generate from https://sentry.io/settings/auth-tokens/)
SENTRY_AUTH_TOKEN=